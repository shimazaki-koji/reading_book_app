<div class="contents row">
  <% @books.each do |book| %>
    <div class="content_post" style="background-image: url(<%= book.image %>);">
      <p><%= book.text %></p>
      <span class="name">
        <%= book.name %>
      </span>
    </div>
  <% end %>
</div>